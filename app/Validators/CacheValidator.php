<?php

namespace App\Validators;

class CacheValidator extends Validator
{
	public function validate()
	{
		// if (! filter_var($this->options->url, FILTER_VALIDATE_URL)) {
		// 	$this->error('Invalid Url');
		// }
	}
}